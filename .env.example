# Copy to .env if you need to override defaults. No API keys required —
# embeddings and detectors run fully locally.

QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=              # only for Qdrant Cloud
COLLECTION=logs
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
